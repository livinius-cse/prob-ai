# Comma-separated origins allowed to access the API during local development.
# No credentials, API keys, or database settings are used in Phase 1.
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
